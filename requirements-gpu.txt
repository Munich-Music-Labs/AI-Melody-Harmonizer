# requirements-gpu.txt:
# Optional GPU add-on for Linux + NVIDIA (CUDA 12). Wheels are not available
# for macOS or Windows arm64. Install on top of requirements.txt:
#
#   pip install -r requirements.txt -r requirements-gpu.txt
#
# Mirrors the [project.optional-dependencies].gpu group in pyproject.toml.

nvidia-cublas-cu12>=12.9.2.10
nvidia-cuda-nvrtc-cu12>=12.9.86
nvidia-cudnn-cu12>=9.20.0.48
